#include <iostream>
#include <algorithm>
#include <iterator>
#include <vector>
using namespace std;

int main(void)
{
	//vector<int> a = { 1,2,3,4,5 };
	//for (int i = 0; i < a.size(); i++) {
	//	if (i == 3) {
	//		a.push_back(4);
	//	}
	//	cout << a[i] << endl;
	//}
	//vector<int> b(10);
	//copy(a.begin(), a.end(), b.begin());
	//auto iter = a.begin();
	//while (iter != a.end())
	//{
	//	cout << *iter << endl;
	//	if (*iter == 3) {
	//		iter = a.insert(a.end(), 6);
	//	}
	//	iter++;
	//}
	//for (int x : b) {
	//	cout << x;
	//}
	//system("pause");
	if (2 == 2&&2 == 2)
		printf("fuck");
	system("pause");
	return 0;
}